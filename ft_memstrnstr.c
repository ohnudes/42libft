#ifndef T_SIZE_T
typedef unsigned int T_SIZE_T ;
#endif 

char	*strnstr(const char *big, const char *little, T_SIZE_T len)
{
	T_SIZE_T counter;
	
	counter = 0;
	if (little[counter] != 0)
	{
		while (counter < len)
		{

		}
		return(0);

	}
	return(big);
}
